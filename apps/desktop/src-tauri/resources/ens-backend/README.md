# Generated backend sidecar

The release build replaces this directory with the PyInstaller onedir output. Generated binaries are ignored by Git. Run `npm run desktop:sidecar` before a direct release Cargo build; `npm run desktop:build` does this automatically.

